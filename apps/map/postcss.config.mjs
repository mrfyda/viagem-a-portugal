// Tailwind CSS v4 is a PostCSS plugin (NativeWind v5). This processes the
// `@import "tailwindcss/..."` directives in global.css for the web build.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
